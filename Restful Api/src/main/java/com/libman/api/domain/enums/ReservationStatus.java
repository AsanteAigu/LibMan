package com.libman.api.domain.enums;

/** Mirrors reservation_status AS ENUM ('waiting','notified','expired','cancelled','fulfilled'). */
public enum ReservationStatus {
    waiting,
    notified,
    expired,
    cancelled,
    fulfilled
}
