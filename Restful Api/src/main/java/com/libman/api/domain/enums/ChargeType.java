package com.libman.api.domain.enums;

/** Mirrors charge_type AS ENUM ('late_fee','damage','lost','ebook_grace_expiry','membership_fee'). */
public enum ChargeType {
    late_fee,
    damage,
    lost,
    ebook_grace_expiry,
    membership_fee
}
