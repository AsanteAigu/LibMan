package com.libman.api.domain.enums;

/** Mirrors ebook_loan_status AS ENUM ('active','grace','returned','removed'). */
public enum EbookLoanStatus {
    active,
    grace,
    returned,
    removed
}
