package com.libman.api.domain.enums;

/** Mirrors copy_status AS ENUM ('available','on_hold','on_loan','withdrawn'). */
public enum CopyStatus {
    available,
    on_hold,
    on_loan,
    withdrawn
}
