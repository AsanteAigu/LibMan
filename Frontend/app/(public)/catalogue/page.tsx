import { CatalogueView } from "@/features/catalogue/catalogue-view";

export default function CataloguePage() {
  return <CatalogueView heading="Catalogue" subheading="Browse the full collection" />;
}
