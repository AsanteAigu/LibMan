import { CatalogueView } from "@/features/catalogue/catalogue-view";

export default function SearchPage() {
  return <CatalogueView heading="Search" subheading="Find a title, author, or subject" />;
}
